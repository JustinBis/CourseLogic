MongoDB Schema Info: (in Mongoose syntax)

var classDataSchema = new Schema({
	schoolID: String,
	schoolName: String,
	subjects: [subjectsSchema],
	classTopics: [classTopicsSchema],
	classOptions:[classOptionsSchema]
});

var subjectsSchema = new Schema({
	subjectID: String,
	subjectName: String
});

var classTopicsSchema = new Schema({
	subjectID: String,
	classID: String,
	className: String
});

var classOptionsSchema = new Schema({
	crn: String,
	classID: String,
	prof: String,
	times: [classTimesSchema]
});

var classTimesSchema = new Schema({
	days: String,
	location: String,
	time: String,
	type: String
});